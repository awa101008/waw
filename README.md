當啟動bot.py時，會讀取"/root/discord-bot"裡面的RP.josh作為RP設定
使用方式：
1. cd到bot.py的位置
2. sudo apt update && sudo apt upgrade -y
3. sudo apt install python3 python3-pip python3-pil -y
4. pip3 install discord.py google-genai --break-system-packages
5. export GEMINI_API_KEY="你的api"
6. python3 bot.py
